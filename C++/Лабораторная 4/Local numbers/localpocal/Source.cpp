
#include <iostream>
int main() {
    /*const int k = 3, g = 3;
    int count = 0;
    int a[k+2][g+2];
    for (int i = 0; i < k + 2; i++)
    {
        for (int j = 0; j < g + 2; j++)
        {
            if ((j == (g + 1) or (i == 0) or (j == 0) or (i == (k + 1)))) {
                a[i][j] = 100;
            }
        }
    }
    for (int i = 1; i < k + 1; i++)
    {
        for (int j = 1; j < g + 1; j++)
        {
            if ((j != (g) or (i != 0) or (j != 0) or (i != (k)))) {
                std::cin >> a[i][j];
             }
        }
    }

    
    for (int i = 1; i < k + 1; i++) {
        for (int j = 1; j < g + 1; j++) {
            if (a[i][j] < a[i - 1][j] && a[i][j] < a[i + 1][j] && a[i][j] < a[i][j - 1] && a[i][j] < a[i][j + 1]) {
                count++;
            }
        }
    }
    std::cout << count;*/
    int mat[2][]={1,2,3};

    return 0;
}